include <stdio.h>
#include <stdlib.h>

int main()
{
    int n, i;
    int *arr1, *arr2;
    printf(reg no:25331A05H5 \n”);
    printf("Enter number of elements: ");
    scanf("%d", &n);

    
    arr1 = (int *)malloc(n * sizeof(int));

   
    arr2 = (int *)calloc(n, sizeof(int));

    
    printf("Enter elements for malloc array:\n");
    for(i = 0; i < n; i++)
    {
        scanf("%d", &arr1[i]);
    }

    
    printf("Elements in malloc array:\n");
    for(i = 0; i < n; i++)
    {
        printf("%d ", arr1[i]);
    }

    
    printf("\nElements in calloc array:\n");
    for(i = 0; i < n; i++)
    {
        printf("%d ", arr2[i]);
    }

    
    free(arr1);
    free(arr2);

    return 0;
}
